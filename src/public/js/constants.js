/**
 * the url to direct users to for login
 * @type {string}
 */
export const LOGIN_URL = "";

/**
 * the base url to use for api requests e.g., https://example.com
 * @type {string}
 */
export const API_URL = "";

/**
 * the base url to use for websocket requests e.g., ws://example.com
 * @type {string}
 */
export const WS_URL = "";

/**
 * the domain for amazon cognito, e.g. example.com.
 * @type {string}
 */
export const AUTH_DOMAIN = "";

/**
 * the client id for amazon cognito
 * @type {string}
 */
export const AUTH_CLIENT_ID = "";
