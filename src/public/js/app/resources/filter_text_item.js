/**
 * Describes a filter that applies to a text field. The name of the field is
 * from context.
 *
 * @typedef {{operator: import("/js/app/resources/standard_text_operator.js").StandardTextOperator, value: string}} FilterTextItem
 */


// We need to export something for visual code to consider this a module :/
export function foo() { } // @@type-hint
