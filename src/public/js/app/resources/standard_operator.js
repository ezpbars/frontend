/**
 * Describes a standard operator which can be applied to any comparable field.
 * Options:
 * - eq: equal
 * - neq: not equal
 * - gt: greater than
 * - gtn: greater than or null
 * - gte: greater than or equal
 * - gten: greater than or equal or null
 * - lt: less than
 * - ltn: less than or null
 * - lte: less than or equal
 * - lten: less than or equal or null
 * @typedef {'eq'|'neq'|'gt'|'gtn'|'gte'|'gten'|'lt'|'ltn'|'lte'|'lten'} StandardOperator
 */

// We need to export something for visual code to consider this a module :/
export function foo() { } // @@type-hint
